<Window x:Class="subphimv1.Services.InputBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Input" 
        SizeToContent="WidthAndHeight" 
        WindowStartupLocation="CenterOwner"
        MinWidth="350"
        AllowsTransparency="True" 
        WindowStyle="None" 
        Background="Transparent"
        TextElement.Foreground="{StaticResource CapCut.TextColor}">

    <!-- BẮT ĐẦU SỬA ĐỔI: Áp dụng style tùy chỉnh cho cửa sổ -->
    <Border BorderBrush="{StaticResource CapCut.BorderColor}" 
            BorderThickness="1" 
            Background="{StaticResource CapCut.WindowBackground}"
            CornerRadius="4">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="32"/>
                <!-- Hàng cho Title Bar -->
                <RowDefinition Height="*"/>
                <!-- Hàng cho nội dung -->
            </Grid.RowDefinitions>

            <!-- Thanh tiêu đề tùy chỉnh -->
            <Grid Grid.Row="0" Background="{StaticResource CapCut.PanelBackground}" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock x:Name="TitleText" 
                           Grid.Column="0"
                           Text="{Binding Title, RelativeSource={RelativeSource AncestorType=Window}}" 
                           VerticalAlignment="Center" 
                           Margin="10,0,0,0"
                           FontWeight="SemiBold"/>

                <Button Grid.Column="1" 
                        x:Name="CloseButton" 
                        Content="✕" 
                        Style="{StaticResource WindowCloseButtonStyle}" 
                        Click="CloseButton_Click"/>
            </Grid>

            <!-- Nội dung chính của InputBox -->
            <StackPanel Grid.Row="1" Margin="15">
                <TextBlock x:Name="PromptText" Margin="0,0,0,10" TextWrapping="Wrap"/>
                <TextBox x:Name="ResponseTextBox" MinWidth="250"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
                    <Button Content="OK" IsDefault="True" Click="OKButton_Click" MinWidth="60" Margin="0,0,10,0"/>
                    <Button Content="Cancel" IsCancel="True" Click="CancelButton_Click" MinWidth="60"/>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Border>
</Window>