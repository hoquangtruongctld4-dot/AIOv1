<Window x:Class="subphimv1.EngineDownloadProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:subphimv1"
        mc:Ignorable="d"
        Title="Đang tải Engine" Height="180" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        ShowInTaskbar="False" Closing="Window_Closing"
        Style="{StaticResource BorderlessToolWindowStyle}"
        MouseDown="Window_MouseDown">
    <Window.Resources>
        <Style TargetType="ProgressBar">
            <Setter Property="Foreground" Value="{StaticResource AccentColor}"/>
            <Setter Property="Background" Value="{StaticResource PanelBackgroundColor}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="20"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Border BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="2">
                            <Grid x:Name="PART_Track">
                                <Rectangle x:Name="PART_Indicator" 
                                           HorizontalAlignment="Left" 
                                           Fill="{TemplateBinding Foreground}" />
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="TitleTextBlock" Grid.Row="0" Text="Đang tải..." FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10" Foreground="{StaticResource TextColor}"/>

        <StackPanel Grid.Row="1" VerticalAlignment="Center">
            <TextBlock x:Name="StatusTextBlock" Text="Vui lòng chờ..." HorizontalAlignment="Center" Foreground="{StaticResource TextSecondaryColor}"/>
            <ProgressBar x:Name="DownloadProgressBar" Margin="0,5,0,0"/>
        </StackPanel>

        <Button x:Name="CancelButton" Grid.Row="2" Content="Hủy bỏ" Width="80" HorizontalAlignment="Right" Click="CancelButton_Click"/>
    </Grid>
</Window>