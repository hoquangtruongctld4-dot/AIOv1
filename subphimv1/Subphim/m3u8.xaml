<Window x:Class="subphimv1.m3u8"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="M3U8 Downloader Tool" Height="500" Width="850" MinWidth="600" MinHeight="400"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        Style="{StaticResource BorderlessToolWindowStyle}"
        MouseDown="Window_MouseDown">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Tiêu đề tùy chỉnh -->
        <TextBlock Grid.Row="0" 
                   Text="{Binding Title, RelativeSource={RelativeSource AncestorType=Window}}" 
                   FontSize="16" FontWeight="SemiBold" 
                   Margin="0,0,0,10"
                   Foreground="{StaticResource TextColor}"/>

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Label Content="Chọn Ổ đĩa để lưu:" VerticalAlignment="Center"/>
            <TextBlock x:Name="FolderLabel" Grid.Column="1" Text="(chưa chọn)" Foreground="{StaticResource TextSecondaryColor}" VerticalAlignment="Center" Margin="5,0" TextTrimming="CharacterEllipsis"/>
            <Button Grid.Column="2" Content="Chọn Ổ Save" Padding="10,3" Click="BrowseFolder_Click"/>
        </Grid>

        <Grid Grid.Row="2" Margin="0,0,0,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Label Content="Tên phim" FontWeight="Bold" Foreground="{StaticResource TextColor}"/>
            <Label Grid.Column="1" Content="Link m3u8" FontWeight="Bold" Foreground="{StaticResource TextColor}"/>
        </Grid>

        <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto" Margin="0,0,0,5" BorderBrush="{StaticResource BorderColor}" BorderThickness="1">
            <StackPanel x:Name="EntryRowsPanel" Orientation="Vertical" Margin="5"/>
        </ScrollViewer>

        <Button Grid.Row="4" Content="+ Thêm dòng để nhập link" Margin="0,10,0,5" Padding="8,4" HorizontalAlignment="Left" Click="AddEntryRowButton_Click"/>

        <CheckBox x:Name="MergeMp4CheckBox" Grid.Row="5" Content="Ghép thành file MP4 sau khi tải xong" Margin="0,10,0,5" IsChecked="False"/>

        <Button x:Name="StartDownloadButton" Grid.Row="6" Content="Bắt đầu tải" Foreground="White" Padding="15,7" FontWeight="Bold" Click="StartDownload_Click" Margin="0,10,0,10" HorizontalAlignment="Center"/>

        <!-- *** THAY ĐỔI TẠI ĐÂY *** -->
        <DockPanel Grid.Row="7" Margin="0,5,0,0">
            <!-- StatusBar sẽ chiếm hết không gian còn lại ở bên trái -->
            <StatusBar DockPanel.Dock="Left">
                <StatusBarItem>
                    <TextBlock x:Name="StatusLabel" Text="Trạng thái: Chờ..." Foreground="{StaticResource AccentColor}"/>
                </StatusBarItem>
            </StatusBar>

            <!-- Nút Đóng sẽ nằm ở bên phải -->
            <Button DockPanel.Dock="Right" 
                    Content="Đóng" 
                    Width="80" 
                    HorizontalAlignment="Right" 
                    Click="CloseButton_Click"/>
        </DockPanel>
    </Grid>
</Window>