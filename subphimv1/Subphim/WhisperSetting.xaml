<Window x:Class="subphimv1.WhisperAdvancedSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:subphimv1"
        mc:Ignorable="d"
        Title="Tham số Dòng lệnh Nâng cao cho Whisper" Height="450" Width="600"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Style="{StaticResource BorderlessToolWindowStyle}"
        MouseDown="Window_MouseDown">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Hàng cho tiêu đề -->
            <RowDefinition Height="*"/>
            <!-- Hàng cho TabControl -->
            <RowDefinition Height="Auto"/>
            <!-- Hàng cho nút Đóng -->
        </Grid.RowDefinitions>

        <!-- Tiêu đề tùy chỉnh -->
        <TextBlock Grid.Row="0" Text="{Binding Title, RelativeSource={RelativeSource AncestorType=Window}}" 
                   FontSize="16" FontWeight="SemiBold" 
                   Margin="0,0,0,10"
                   Foreground="{StaticResource TextColor}"/>

        <TabControl Grid.Row="1">
            <TabItem Header="CPP / Const-me">
                <TextBox x:Name="CppArgsTextBox" IsReadOnly="true" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" FontFamily="Consolas" TextWrapping="Wrap" Padding="5"/>
            </TabItem>
            <TabItem Header="CTranslate2 / Faster-Whisper">
                <TextBox x:Name="CTranslate2ArgsTextBox" IsReadOnly="true" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" FontFamily="Consolas" TextWrapping="Wrap" Padding="5"/>
            </TabItem>
        </TabControl>

        <Button Grid.Row="2" Content="Đóng" Width="80" HorizontalAlignment="Right" Margin="0,15,0,0" IsCancel="True"/>
    </Grid>
</Window>