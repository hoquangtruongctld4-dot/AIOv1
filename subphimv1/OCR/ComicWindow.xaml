<Window x:Class="subphimv1.OcrComicWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:subphimv1"
        mc:Ignorable="d"
        Title="Tool OCR Dịch Truyện Tranh" Height="800" Width="1200"
        MinHeight="700" MinWidth="1000"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True"
        Background="{StaticResource WindowBackgroundColor}"
        Foreground="{StaticResource TextColor}"
        FontFamily="Segoe UI" 
        Closing="Window_Closing">

    <Border BorderBrush="{StaticResource BorderColor}" BorderThickness="1" CornerRadius="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Thanh tiêu đề (Không thay đổi) -->
            <Grid Grid.Row="0" x:Name="TitleBar" MouseDown="TitleBar_MouseDown" Background="Transparent">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="15,0">
                    <TextBlock Text="" FontFamily="Segoe MDL2 Assets" FontSize="16" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBlock Text="{Binding Title, RelativeSource={RelativeSource AncestorType=Window}}" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Style="{StaticResource WindowButtonStyle}" Content="—" Click="MinimizeButton_Click"/>
                    <Button Style="{StaticResource WindowButtonStyle}" Content="☐" Click="MaximizeButton_Click"/>
                    <Button Style="{StaticResource WindowCloseButtonStyle}" Content="✕" Click="CloseButton_Click"/>
                </StackPanel>
            </Grid>

            <!-- NỘI DUNG CHÍNH - BỐ CỤC MỚI -->
            <Grid Grid.Row="1" Margin="15">
                <Grid.ColumnDefinitions>
                    <!-- CỘT 1: Chứa toàn bộ phần cài đặt -->
                    <ColumnDefinition Width="350"/>
                    <!-- CỘT 2: Không gian làm việc chính (Ảnh và Kết quả) -->
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- =============================================================== -->
                <!-- CỘT 1: BẢNG ĐIỀU KHIỂN & CÀI ĐẶT                             -->
                <!-- =============================================================== -->
                <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto" Margin="0,0,15,0">
                    <StackPanel>
                        <GroupBox Header="Tác vụ chính">
                            <StackPanel>
                                <WrapPanel Margin="0,5,0,10">
                                    <Button x:Name="SelectImageButton" Content="Chọn Ảnh" Click="SelectImageButton_Click" Width="150" Height="33"/>
                                    <Button x:Name="OCRAndTranslateButton" Content="OCR Dịch Ảnh" Click="OCRAndTranslateButton_Click" Width="150" Margin="5,0,0,0"/>
                                </WrapPanel>
                                <WrapPanel>
                                    <Button x:Name="SelectFolderOCRButton" Content="Chỉ OCR Thư Mục" Click="OCRFolderButton_Click" Width="150" Height="35"/>
                                    <Button x:Name="SelectFolderButton" Content="OCR Dịch Thư Mục" Click="OCRAndTranslateAllButton_Click" Width="150" Margin="5,0,0,0" />
                                </WrapPanel>
                            </StackPanel>
                        </GroupBox>

                        <GroupBox Header="Cấu hình API Models" Margin="0,10,0,0">
                            <StackPanel>
                                <Label Content=" API Keys:" Foreground="{StaticResource TextSecondaryColor}"/>
                                <Button x:Name="ManageApiKeysButton" Height="34" HorizontalAlignment="Stretch" Click="ManageApiKeysButton_Click" Margin="0,5,0,10" Width="199">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="" FontFamily="Segoe MDL2 Assets" FontSize="16" VerticalAlignment="Center"/>
                                        <TextBlock Text="API KEY" Margin="10,0,0,0" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>

                                <Label Content="Model OCR:" Foreground="{StaticResource TextSecondaryColor}"/>
                                <ComboBox x:Name="OcrModelComboBox" Margin="0,5,0,10" Height="33" Width="216">
                                    <ComboBoxItem Content="gemini-2.5-flash"/>
                                    <ComboBoxItem Content="gemini-2.5-flash-lite-preview-06-17"/>
                                    <ComboBoxItem Content="learnlm-2.0-flash-experimental" IsSelected="True"/>
                                </ComboBox>

                                <Label Content="Model Dịch:" Foreground="{StaticResource TextSecondaryColor}"/>
                                <ComboBox x:Name="TranslateModelComboBox" Margin="0,5,0,0" Width="219" Height="29">
                                    <ComboBoxItem Content="gemini-2.5-flash" IsSelected="True"/>
                                    <ComboBoxItem Content="gemini-2.5-flash-lite-preview-06-17"/>
                                    <ComboBoxItem Content="gemini-2.0-flash-lite"/>
                                    <ComboBoxItem Content="gemini-2.0-flash"/>
                                </ComboBox>
                            </StackPanel>
                        </GroupBox>

                        <GroupBox Header="Tùy chọn Dịch" Margin="0,10,0,0">
                            <StackPanel>
                                <TextBlock Text="Thể loại / Hướng đọc:" FontWeight="SemiBold" Margin="0,5,0,5"/>
                                <ComboBox x:Name="GenreComboBox" Margin="5,0,0,10" SelectedIndex="0" Width="299">
                                    <ComboBoxItem Content="Manhwa / Webtoon (Trái->Phải, Dài)"/>
                                    <ComboBoxItem Content="Tiêu chuẩn / Đô thị (Trái->Phải)"/>
                                    <ComboBoxItem Content="Tiên Hiệp / Kiếm Hiệp (Trái->Phải)"/>
                                    <ComboBoxItem Content="Manga (Phải->Trái)"/>
                                </ComboBox>

                                <TextBlock Text="Dịch sang:" FontWeight="SemiBold" Margin="0,0,0,5"/>
                                <StackPanel Orientation="Horizontal" Margin="5,0,0,0">
                                    <ComboBox x:Name="LanguageComboBox" Width="94" Height="27">
                                        <ComboBoxItem Content="Tiếng Việt" IsSelected="True"/>
                                        <ComboBoxItem Content="Tiếng Anh"/>
                                    </ComboBox>
                                    <Button x:Name="CustomPromptButton" ToolTip="Prompt Dịch Tùy Chỉnh" Click="CustomPromptButton_Click" Width="55" Height="34" Margin="10,0,0,0">
                                        <TextBlock Text="" FontFamily="Segoe MDL2 Assets" FontSize="16"/>
                                    </Button>
                                </StackPanel>
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>
                </ScrollViewer>

                <!-- =============================================================== -->
                <!-- CỘT 2: KHÔNG GIAN LÀM VIỆC (ẢNH + KẾT QUẢ)                  -->
                <!-- =============================================================== -->
                <Grid Grid.Column="1">
                    <Grid.RowDefinitions>
                        <!-- HÀNG 1: Khu vực hiển thị ảnh, chiếm nhiều không gian hơn -->
                        <RowDefinition Height="2*"/>
                        <!-- HÀNG 2: Khu vực hiển thị kết quả OCR và Dịch -->
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- KHU VỰC ẢNH LỚN HƠN -->
                    <Border Grid.Row="0" Background="{StaticResource PanelBackgroundColor}" CornerRadius="4" Margin="0,0,0,15">
                        <Image x:Name="SelectedImage" Stretch="Uniform"/>
                    </Border>

                    <!-- KHU VỰC KẾT QUẢ -->
                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- CỘT 2.1: KẾT QUẢ OCR -->
                        <Border Grid.Column="0" Background="{StaticResource PanelBackgroundColor}" CornerRadius="4" Margin="0,0,7.5,0">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <TextBlock Text="Kết quả OCR" FontWeight="Bold" Margin="10,8" Foreground="{StaticResource TextSecondaryColor}"/>
                                <TextBox Grid.Row="1" x:Name="OcrResultTextBox" TextWrapping="Wrap" AcceptsReturn="True" 
                                         VerticalScrollBarVisibility="Auto" VerticalContentAlignment="Top" 
                                         Background="Transparent" BorderThickness="0" Padding="10,0,10,10"/>
                            </Grid>
                        </Border>

                        <!-- CỘT 2.2: KẾT QUẢ DỊCH -->
                        <Border Grid.Column="1" Background="{StaticResource PanelBackgroundColor}" CornerRadius="4" Margin="7.5,0,0,0">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <TextBlock Text="Kết quả Dịch" FontWeight="Bold" Margin="10,8" Foreground="{StaticResource TextSecondaryColor}"/>
                                <TextBox Grid.Row="1" x:Name="TranslatedResultTextBox" TextWrapping="Wrap" AcceptsReturn="True" 
                                         VerticalScrollBarVisibility="Auto" VerticalContentAlignment="Top" 
                                         Background="Transparent" BorderThickness="0" Padding="10,0,10,10"/>
                            </Grid>
                        </Border>
                    </Grid>
                </Grid>
            </Grid>

            <!-- Thanh trạng thái (Không thay đổi) -->
            <StatusBar Grid.Row="2">
                <StatusBarItem>
                    <TextBlock x:Name="StatusTextBlock" Text="Sẵn sàng" Margin="10,0"/>
                </StatusBarItem>
                <StatusBarItem HorizontalAlignment="Right">
                    <ProgressBar x:Name="MainProgressBar" Width="200" Height="12" Margin="0,0,10,0" Visibility="Collapsed"/>
                </StatusBarItem>
            </StatusBar>
        </Grid>
    </Border>
</Window>